/**
 * LazyLoadingFallback - Completely invisible
 * Keeps previous page visible while new page loads in background
 * Smooth transition when content is ready
 */
const LazyLoadingFallback = () => null;

export default LazyLoadingFallback;
