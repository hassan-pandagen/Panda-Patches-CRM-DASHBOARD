import { useEffect } from 'react';

/**
 * This component listens for specific auth events from Supabase that require navigation.
 * It must be rendered within a Router context to use `useNavigate`.
 */
const AuthListener: React.FC = () => {
  useEffect(() => {
    // This component is now a placeholder.
    // The password recovery flow has been removed.
  }, []);

  return null; // This component does not render anything visible.
};

export default AuthListener;
