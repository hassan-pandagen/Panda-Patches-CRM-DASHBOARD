fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pillow>=10.0
numpy>=1.26
opencv-python-headless>=4.9
requests>=2.31
vtracer>=0.6            # raster -> SVG for PVC/silicone/leather/woven vectors
rq>=1.16                # job queue — single worker processes mockup jobs one at a time
redis>=5.0               # rq's backing store
pdf2image>=1.17          # rasterize PDF/AI (PDF-compatible) uploads — needs poppler-utils (system pkg)
# Test suite (pytest -q)
pytest>=8.0
httpx>=0.27              # required by fastapi.testclient.TestClient
# Optional — photoreal finishing pass (paid, ~$0.03/MP). Set env FAL_KEY to enable:
# fal-client>=0.5
# Optional — better background removal for photo uploads (~200MB extra):
# rembg>=2.0
# onnxruntime>=1.18
